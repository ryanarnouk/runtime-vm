// Potentially will use hash map to store the actual code of each method and have a reference to it directly from
// the constant pool based on the name index in the CP rather than the string of the name as the current implementation does
